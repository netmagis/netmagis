#
# Example crontab for mkroute script: generates mail route database
# This crontab should be installed on mail relays.
#

MAILTO = hostmaster@example.com

# use flag -q if status messages are not wanted
# specify view name if you want to limit generation to a particular view
*/1	* * * *	%BINDIR%/mkmroute
