#
# Example crontab for mkdhcp script: generates DHCP configuration file
# for ISC dhcpd.
# This crontab should be installed on DHCP server.
#

MAILTO = hostmaster@example.com

# use flag -q if status messages are not wanted
# specify view name if you want to limit generation to a particular view
1-59/1 * * * * %BINDIR%/mkdhcp
