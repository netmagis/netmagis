#
# Example crontab for mkzones script: generates zones for BIND.
# This crontab should be installd on DNS authoritative server.
#

MAILTO = hostmaster@example.com

# use flag -q if status messages are not wanted
# use flag -w if you want to limit generation to a particular view
*/1 * * * * %SBINDIR%/mkzones
